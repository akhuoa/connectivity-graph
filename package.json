{
  "name": "path-graph",
  "description": "Show neuron connectivity graphs on a canvas",
  "version": "0.2.3",
  "author": "David Brooks",
  "license": "MIT",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "vite serve --port 3002",
    "preview": "npm run build && vite preview",
    "build": "vite build . --base /connectivity-graph/"
  },
  "dependencies": {
    "cytoscape": "^3.29.2",
    "cytoscape-dagre": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.2.2",
    "vite": "^5.1.4"
  }
}
